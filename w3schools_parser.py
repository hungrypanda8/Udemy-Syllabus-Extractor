"""W3Schools HTML parser (placeholder).

Implemented in Phase 2.3.  Will expose ``parse(html_path: str) -> dict``
returning the shared intermediate dict shape (``title``, ``objectives``,
``syllabus``) so the markdown generator stays source-agnostic.
"""
